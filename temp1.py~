# ფუნქციის გამოცხადება
 def meth(n):
     print('რეკურსიის სიღრმე {}'.format(n))
     if n > 150:
         # პროგრამა როგორც კი მიაღწევს return-ს დააბრუნებს მნიშვნელობას და გამოდის ფუნქციიდან
         # შესაბამისად ქვემოთ მოცემული რეკურსიაც აღარ შესრულდება
         return 0

     else:
         # n ცვლადის ინკრემენტი. თუ იყო ერთი შემდგომი მნიშვნელობა გახდება 2
         n+=1
         # ფუნქცია მიმართავს თავის თავს, რომელსაც გადაეცემა ინკრემენტირებული ცვლადის მნიშვნელობა
         meth(n)

# ფუნქციის გამოძახება
meth()
